<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
	<li>
      <%= status_icon user.online? %>
	  <%= link_to user.name.capitalize, user %>
      <%= 'id: ' + user.id.to_s %>

      <% if current_user.admin? && !current_user?(user) %>
        <%= link_to 'delete', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'delete_chat' %>
      <% end %>
	</li>
  <% end %>
</ul>

<%= will_paginate %>