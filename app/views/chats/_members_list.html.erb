<% members.each do |member| %>
    <%= member[:name] %>
    <% member[:is_online] ? icon = 'Status-user-online-icon.png' : icon = 'Status-user-busy-icon.png' %>
    <%= image_tag icon %>
<% end %>